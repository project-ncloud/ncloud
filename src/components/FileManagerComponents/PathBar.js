import React from "react";

function PathBar({ path }) {
  const x = "";
  x.replace();
  return (
    <div className="fPathBar">
      {/* <p>{path.replace(/\//g, " > ")}</p> */}
      <p>{path}</p>
    </div>
  );
}

export default PathBar;
